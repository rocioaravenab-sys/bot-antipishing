# Discord (bot)
discord.py>=2.3.0

# API REST (app móvil)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Configuración
python-dotenv>=1.0.0

# OCR y QR
pytesseract>=0.3.10        # requiere el binario 'tesseract' instalado en el sistema
Pillow>=10.0.0
opencv-python-headless>=4.8.0   # preprocesado de imagen para mejorar OCR/QR
pyzbar>=0.1.9              # requiere 'zbar' instalado en el sistema

# Análisis de URLs y teléfonos
requests>=2.31.0
tldextract>=5.0.0
phonenumbers>=8.13.0       # validación del número remitente (tipo de línea, región)
